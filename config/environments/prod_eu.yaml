# Production EU Region Environment Configuration
# This file contains environment-specific settings for EU production region

environment:
  name: production-eu
  description: Production EU Region environment
  region: eu-west-1
  debug_mode: false

# Nexpose/Console Configuration
nexpose:
  host: eu-nexpose.rapid7.com
  port: 3780
  protocol: https
  ssl_verify: true
  timeout:
    default: 120
    login: 300
    scan: 10800
    network: 180

# API Configuration
api:
  base_path: /api
  version: "3"
  retry:
    max_attempts: 3
    interval: 15
    backoff_multiplier: 2

# Logging Configuration
logging:
  level: INFO
  console_level: WARN
  file_level: INFO
  format: "[%(asctime)s] [%(levelname)s] [%(name)s] [EU] %(message)s"
  date_format: "%Y-%m-%d %H:%M:%S"
  rotate_size_mb: 100
  rotate_count: 20

# Test Execution Configuration
execution:
  parallel_workers: 8
  default_timeout: 120
  include_tags:
    - smoke
    - critical
    - production
    - eu-region
  exclude_tags:
    - manual
    - debug
    - performance
    - development
    - us-only

# Results Configuration
results:
  base_dir: results/prod-eu
  reports_dir: results/prod-eu/reports
  logs_dir: results/prod-eu/logs
  screenshots_dir: results/prod-eu/screenshots
  artifacts_dir: results/prod-eu/artifacts

# Cleanup Configuration
cleanup:
  skip_cleanup: false
  auto_delete_resources: true
  preserve_logs_on_failure: true

# Feature Flags
features:
  dry_run: false
  capture_screenshots: true
  detailed_logging: false
  performance_monitoring: true

# Security Settings
security:
  high_security_mode: true
  alerts_enabled: true
  audit_logging: true
  gdpr_compliance: true
